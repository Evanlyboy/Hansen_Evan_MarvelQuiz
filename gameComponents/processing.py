def questionSort(answer):
	if answer == "good":
		characters.characterCount["spider"] + 1
		characters.characterCount["cyclops"] + 1
	elif answer == "bad":
		characters.characterCount["mysterio"] + 1
		characters.characterCount["carnage"] + 1
	elif answer == "social":
		characters.characterCount["spider"] + 1
		characters.characterCount["cyclops"] + 1
	elif answer == "isolated":
		characters.characterCount["mysterio"] + 1
		characters.characterCount["carnage"] + 1
	elif answer == "calm":
		characters.characterCount["spider"] + 1
		characters.characterCount["mysterio"] + 1
	elif answer == "erratic":
		characters.characterCount["cyclops"] + 1
		characters.characterCount["carnage"] + 1
	elif answer == "acquired":
		characters.characterCount["spider"] + 1
		characters.characterCount["mysterio"] + 1
		characters.characterCount["carnage"] + 1
	elif answer == "latent":
		characters.characterCount["cyclops"] + 1